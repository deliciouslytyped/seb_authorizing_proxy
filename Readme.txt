This is meant to be as self-contained as reasonable, see the source.

SafeExamBrowser is not provided for linux platforms.
This tool was built to work around that;

This is an authorizing proxy implemented with mitmproxy, taking a SEB
config file and providing an HTTP proxy, which can be used to access
SafeExamBrowser protected tests from a normal browser.
